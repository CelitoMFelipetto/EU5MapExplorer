{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path'); if [[ \"$FILE_PATH\" == *.cs ]]; then jb cleanupcode \"$FILE_PATH\" --profile=\"Built-in: Reformat Code\" --no-build-check; fi"
          }
        ]
      }
    ]
  }
}